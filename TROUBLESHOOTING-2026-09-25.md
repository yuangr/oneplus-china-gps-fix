# Ace 6 / Evolution X GNSS 排障记录（2026-09-25）

适用对象：一加 Ace 6（PLQ110 / SM8750），Evolution X Android 17（SDK 37），APatch，GPSFix v1.4.8。

本文是一次实机排障的可复用记录：保留观察、判断条件、实际修复和未证实项；它不是对内部推理过程的逐字记录，也不应把一次测试结论泛化到其他 ROM 或设备。

## 现象与基线

- 用户在室外、移动数据条件下，高德首次导航曾约 52.6 秒才稳定定位，且约 1 分钟后才消失“定位信号弱”。
- 刷回 ColorOS 后导航正常；同机 Evolution X 的 GPSTest 也曾迟迟不能首次定位。因此优先检查 ROM 的 GNSS 配置、SELinux 域转换、辅助数据（XTRA/PSDS）与权限，而不是先怀疑天线或高德。
- 不能把“高德地图有位置”直接当作卫星定位成功：高德可以使用网络或融合位置。必须同时查看 `dumpsys location` 中高德的 `gps provider` 注册、位置回报数、GNSS 测量数、卫星数/CN0 与 TTFF。

## 排障顺序

1. **先记录真实表现，不改系统。** 在空旷室外，记录打开高德导航的时间、首次位置稳定时间、是否有弱信号提示、网络类型；同场地用 GPSTest 交叉验证。
2. **确认应用是否真的申请并收到 GNSS。**

   ```sh
   adb shell dumpsys location
   ```

   重点看 `com.autonavi.minimap` 的 `gps` 注册、`locations`、`GNSS measurement events`；看 `gps provider` 的 `last location`、精度、卫星数及 `GNSS_KPI` 的 TTFF。
3. **检查配置叠加是否破坏原厂射频参数。** 类原生 ROM 的 `/odm/etc/gps.conf`、`/system/etc/gps_debug.conf` 必须以该 ROM 的完整原始文件为基础做最小差量修改。禁止把短小“补丁 gps.conf” bind mount 到完整硬件配置上，否则会丢失 RF、星座和多频参数。
4. **检查 SELinux 与守护进程。** `loc_launcher` 拉起的 `xtra-daemon`、`lowi-server` 应进入 ROM 原有专用域；观察 AVC，而不是用宽泛 allow 或让进程长期停留在通用 `vendor_location` 域。
5. **检查辅助数据目录。** `/data/vendor/location/xtra` 及 `xtra.sqlite` 必须是 `gps:gps`，标签为 `u:object_r:vendor_location_data_file:s0`。错误的 `root:root` 所有者会使数据库打开失败，即使文件存在也无法正常工作。
6. **最后才检查厂商 QCC 组件。** 确认 `com.qti.qcc` 是否加载其所需库；只要库来自不同 ROM/版本，就必须视作实验性改动，安装后用进程 maps、SELinux 标签、实际 TTFF 验证，并预留 APatch 禁用模块/安全模式回退。

## 本次已确认的原因与修复

### 1. XTRA 数据库目录权限错误

实机上 `/data/vendor/location/xtra` 和数据库曾为 `root:root`，日志显示数据库无法打开。已改为 `gps:gps`，保留原有 0600 权限和 `vendor_location_data_file` 标签；重启定位服务后，数据库打开错误消失。

判断标准不是命令返回成功，而是：QCC/XTRA 日志不再出现数据库打开失败，且数据库时间戳随后被正常服务更新。

### 2. Evolution X 的 QCC 缺少 `libqccdme.so`

日志曾显示 QCC 无法加载 `libqccdme.so`。从用户提供的同机 ColorOS 包提取了 arm64 库，封装为单独 APatch 测试模块 `qcc_xtra_coloros_compat` 并 overlay 到：

```
/system_ext/lib64/libqccdme.so
```

安装后验证：

- 文件 SHA-256 与提取件一致，权限 `0644`、所有者 `root:root`、标签 `system_lib_file`；
- QCC 进程的 `/proc/<pid>/maps` 确实映射了该库；
- 不再出现该库缺失错误。

**风险：** APK 与 ColorOS 库的版本哈希不同，所以这只能说明动态加载成功，不能证明协议/下载链路完全兼容。该模块须保持独立，不能并入通用 GPSFix 正式版，除非完成多个 ROM 版本的冷启动测试。

## 2026-09-25 的验证证据

- 高德在 `14:18:26` 请求 1 秒高精度 GPS，并在 `14:37:39` 结束该会话；保留的 Location Manager 记录中，高德收到了约 `2,464` 个 GPS 位置和 `2,473` 个 GNSS 测量事件。
- 当时本次开机周期的 GNSS KPI：5 次 TTFF，均值 `4.49 s`；位置失败比例约 `0.17%`。
- 最近一次卫星位置精度约 `3.8 m`，可见 28 颗卫星；参与解算的星座包含 GPS、GLONASS、QZSS、北斗和 Galileo，Top-4 平均 C/N0 约 `31.8 dB-Hz`。

结论：该时段的高德实际在用 GNSS，且表现恢复正常。KPI 是开机周期累计值，不能把它误写成“某一次高德启动精确 TTFF”。若需每次启动的精确 TTFF，必须在开始测试前清晰记录时间并保留对应日志。

## 未解决项：`xtra3.bin` 未刷新

`/data/vendor/location/xtra3.bin` 仍显示为 2026-09-21 的旧时间戳。XTRA 数据库权限修复后，QCC 能运行且正常导航已恢复，但尚未获得“新 XTRA 文件下载完成”的证据。

这不是当前导航阻断项：实测 4.49 秒级 TTFF 已证实核心 GNSS 正常。它是冷启动可靠性风险：在长时间关机、清除定位数据、跨区域或星历过期后，首次定位可能再次变慢。

后续应做的事：

1. 在不改动配置的前提下，保留一次开机联网后的 QCC/XTRA 日志，区分“请求已接受”“HTTP 下载成功”“数据已注入”三个状态。
2. 进行至少三次受控冷启动：记录断电/飞行模式时长、场地、网络、启动时间和首次稳定 GPS fix 时间。
3. 若仍无法下载，不要伪造/强行覆盖 XTRA 文件；优先核对 QCC APK、库、服务端 URL、证书、SELinux 拒绝和网络可达性。
4. 只有在多次冷启动仍出现 >30–60 秒或持续弱信号时，才继续扩展修复范围。

## 安全与回退

- 先做只读观测，再做一次最小、可逆的改动；每个改动后重启并测试。
- 不要删除 `/data/vendor/location`，不要为了“刷新星历”盲目清空 A-GPS 数据；这会把问题变成冷启动并丢失诊断证据。
- 若 QCC 兼容模块导致异常，在 APatch 中禁用 `qcc_xtra_coloros_compat` 后重启即可回退；GPSFix v1.4.8 与该实验模块必须能独立启停。
- 提交问题报告时须包含：ROM 构建日期、固件基带版本、是否有 root/模块、高德与 GPSTest 的场地和 TTFF、`dumpsys location` 的相关片段，以及 AVC/QCC/XTRA 错误日志。

## 决策日志：证据 → 判断 → 动作

这部分用于复用排障方法。每个结论均应能被命令输出或实机现象复核；“未知”必须保留为未知，不能用合理猜测替代。

| 阶段 | 可观察证据 | 判断与排除项 | 下一步 / 风险控制 |
|---|---|---|---|
| 发现问题 | 高德与 GPSTest 都出现首次定位慢；ColorOS 同机正常 | 现象跨应用，优先排除单一高德设置问题；ColorOS 正常使硬件天线彻底损坏的概率降低，但不作为唯一结论 | 对比 ROM 的 GNSS 配置、服务与权限，先不改硬件或清数据 |
| 定义“成功” | 应用位置可来自网络、融合或卫星 | “地图有蓝点”不能证明搜星成功 | 用 `dumpsys location` 同时确认高德 GPS 注册、位置回报、GNSS 测量、卫星/CN0 与 TTFF |
| 配置层审计 | 旧模块曾把短小配置文件覆盖到 ROM 完整 `gps.conf` | 覆盖会丢失硬件 RF、多频和星座配置，可能直接影响搜星；不能仅凭 SUPL 地址正确就宣称配置正确 | 改为以当前 ROM 原文件为基线的最小差量合成；重启后再验证挂载和文件内容 |
| SELinux 审计 | `loc_launcher` 启动 XTRA/LOWI 时出现 AVC；让子进程留在通用域后仍出现 Binder/服务管理拒绝 | 问题不是“缺一条宽泛 allow”，而是标准域转换不完整 | 补齐进入 ROM 原有 `xtra-daemon` / `lowi-server` 专用域所需的转换权限；避免扩大策略授权范围 |
| 辅助数据目录 | XTRA 目录与 `xtra.sqlite` 曾为 `root:root`，日志显示数据库打不开 | 即使下载服务存在，数据目录权限错误也会阻断状态维护和下载链路 | 修正为 `gps:gps`，保持原模式和 SELinux 标签；以日志无数据库打开错误、数据库更新时间变化作为验证 |
| QCC 依赖审计 | QCC 日志报 `Failed to load libqccdme.so`；Evolution X 中无该库 | 缺库能解释 QCC 相关能力不可用，但不能直接证明它是全部 TTFF 问题的唯一根因 | 从同机 ColorOS 提取匹配架构库，做独立且可禁用的 APatch 测试模块；不并入正式模块 |
| 兼容模块验证 | 模块重启后，库 SHA-256、权限、SELinux 标签正确；QCC 的 `/proc/<pid>/maps` 显示库被加载 | 仅能证明动态链接与进程加载成功；不能证明 QCC 协议、下载服务或数据注入成功 | 继续看下载完成日志、`xtra3.bin` 更新时间和受控冷启动 TTFF；保留 APatch 一键禁用回退 |
| 缓存刷新验证 | 手机可用 HTTPS 成功下载当前 XTRA3 文件；替换为同格式新缓存后，高德首次 GPS 回报仍约 68 秒 | 网络、服务器和缓存文件格式不是当前阻断点；不能将手工刷新作为正式方案 | 不将手工下载逻辑加回通用模块；继续检查注入/守护进程协作 |
| 生命周期 AVC | 内核记录 `vendor_location` 向 `vendor_location_xtra_daemon` 发送 `signal` 被拒绝；源域覆盖 `loc_launcher` 与 `loc_mq_clnt` | 这会影响 XTRA daemon 的正常协作和回收，是可直接修复的最小权限缺口 | v1.4.8 仅新增该受限 `signal` 规则，安装后重启并复查 AVC 与 TTFF |
| 实际导航复测 | 高德高精度 GPS 会话、GPS 位置回报、GNSS 测量均持续存在；累计 TTFF 均值 4.49 秒；多星座参与解算 | 本次导航已经是正常的卫星定位，不是网络位置伪装；但累计 KPI 不能代表每一次启动的精确 TTFF | 结论限于已测时段。若要衡量冷启动，另行做多轮、明确起止时间的测试 |
| 未闭环项 | `xtra3.bin` 时间仍旧，未得到下载完成和注入完成的端到端证据 | 辅助星历链路尚未完全证实；当前正常导航说明它不是此刻的阻断因素 | 不清空定位数据，不伪造星历；等待或采集后续请求/下载/注入日志，再按冷启动复测决定是否扩大修复 |

### 可复用的判断准则

1. **先区分现象与因果。** “弱信号提示”“应用卡住”“星历文件旧”都是现象；只有服务日志、权限、配置和受控复测相互印证，才可上升为原因。
2. **每次只改一个层级。** 先配置，再 SELinux/权限，最后才是跨 ROM 组件；否则无法确定哪项改动有效，也会令回退困难。
3. **以端到端结果为最高证据。** 文件存在、命令成功、进程存活、库被加载，证据强度依次有限；实际高精度 GPS 回报、TTFF、卫星/CN0 和重复测试才是最终验证。
4. **每个实验性改动必须可独立回退。** 外来 ROM 库不进入通用模块；采用单独模块 ID，并在改动前写明禁用和重启的回退步骤。
5. **不把一次成功过度泛化。** 室外热启动快不等于冷启动、室内、跨城或弱网都快；报告中必须标出场地、网络、启动条件与测量范围。
